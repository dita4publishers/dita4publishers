<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE concept PUBLIC "urn:pubid:dita4publishers.org:doctypes:dita:concept" "urn:pubid:dita4publishers.org:doctypes:dita:concept">
<concept id="conceptId">
  <title>EPUB and Kindle Transformation Parameters</title>
  <shortdesc>EPUB-specific parameters for the EPUB transformation type</shortdesc>
  <prolog>
    <metadata>
      <keywords>
        <indexterm>transformation parameters</indexterm>
      </keywords>
    </metadata>
  </prolog>
  <conbody>
    <p>The EPUB transformation type allows a number of parameters that configure the details of the generated EPUB. Available options include:<ul
      id="ul_afc9168f-c20e-44a4-a21c-8f1317200eb8">
      <li>Include a back-of-the-book index</li>
      <li>The names of the directories used to hold topics, graphics, and CSS style sheets.</li>
      <li>The topic and title element @class values to use for topics generated from topicheads.</li>
      <li>The maximum depth of the generated table of contents.</li>
      <li>The graphic to use as the EPUB cover graphic (&lt;meta name="cover" content="coverimage" /> as used by iBooks) if the cover image is not indicated by markup in the map.</li>
      <li>The EPUB publication ID</li>
      <li>Do not treat external-scope xrefs as links in the HTML (avoids EPUB validation issues with unavailable resources).</li>
      <li>Include user-specified CSS files in the generated EPUB package.</li>
      </ul></p>
    <p>The parameters documented in this section are Ant parameters recognized by the <filepath>build_transtype_epub.xml</filepath> Ant script. Each of these Ant parameters corresponds to an XSLT parameter used by the underlying XSLT transforms. The XSLT parameters are defined in the <filepath>map2epubImpl.xsl</filepath> transform in the EPUB plugin's <filepath>xsl/</filepath> directory.</p>
  </conbody>
</concept>
