<plugin id="net.sourceforge.dita4publishers.xslt.common">
  <!-- This plugin provides shared XSLT components that are used
       by reference from other plugins. It provides no
       transtypes nor does it extend any other plugins.
    -->
</plugin>